local BaseUIComponent = require ('BaseUIComponent')
local ModuleRefer = require('ModuleRefer')
local ConfigRefer = require('ConfigRefer')
local Delegate = require('Delegate')

local I18N = require("I18N")

---@class CityWorkProduceUIOutputItem:BaseUIComponent
local CityWorkProduceUIOutputItem = class('CityWorkProduceUIOutputItem', BaseUIComponent)

function CityWorkProduceUIOutputItem:OnCreate()
    
end

function CityWorkProduceUIOutputItem:OnFeedData(data)
    
end

return CityWorkProduceUIOutputItem