---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yining.zhang.
--- DateTime: 2022/1/12 19:05
--- 相机的操作封装，方面以后进行统一优化

---@class CameraManager
---@field Instance fun():CameraManager
---@field Init fun(self:CameraManager)
---@field RegisterCameraBlendOnceCallback fun(self:CameraManager, time:any, callback:any)
local CinemachineManager = class("CinemachineManager")

local m_Instance = nil

---@return CameraManager
function CinemachineManager.Instance()
    if not m_Instance then
        m_Instance = CinemachineManager.new()
    end

    return m_Instance
end

---@param self CameraManager
---@param time any
---@param callback any
---@return void
function CinemachineManager:RegisterCameraBlendOnceCallback(time, callback)
    --CS.GameInterface.AddGlobalTimer(time, true, callback)
    require("Utilities.Timer.TimerUtility").DelayExecute(callback, time)
end

return CinemachineManager